name: camera_example
description: Demonstrates how to use the camera plugin.

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera:
    path: ../
  path_provider: ^1.6.9
  video_player: ^0.10.0
  e2e: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter

flutter:
  uses-material-design: true
